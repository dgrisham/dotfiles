#!/bin/zsh

wenv_def() {
    WENV_DIR="$SRC/ipfs/go-ipfs"
    WENV_DEPS=()
    WENV_PROJECT=''
    WENV_BRANCH=''
    WENV_EXTENSIONS=('c' 'edit')

    bootstrap_wenv() {}
    startup_wenv() {
        # tmux split-window 'monitor git go test -run TestWork'
    }
    shutdown_wenv() {}
}

declare -Ag wenv_dirs=(
    ['bitswap']='../go-bitswap'
    ['peertaskqueue']='../go-peertaskqueue'
)

declare -Ag wenv_files=()
