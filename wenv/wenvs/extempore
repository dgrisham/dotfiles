#!/usr/bin/env zsh

wenv_def() {
    WENV_DIR="/home/grish/src/pkgbuild/extempore-git"
    WENV_DEPS=()
    WENV_PROJECT=''
    WENV_TASK=''
    WENV_EXTENSIONS=()

    startup_wenv() {}
    shutdown_wenv() {}
    bootstrap_wenv() {}
}

# for c extension
# declare -Ag wenv_dirs=()

# for edit extension
# declare -Ag wenv_files=()
